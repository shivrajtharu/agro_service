class User {
  String email;
  String password;
  String username;
  String fullName;
  String address;
  // String username;
  User(this.email, this.password, this.username, this.fullName, this.address
      // this.username
      );
}